services:
  frontend:
    build:
      context: ./Frontend
      args:
        # This value is embedded into the built frontend bundle and used by the *browser*.
        VITE_API_URL: "http://localhost:8080"
    ports:
      - "5173:80"
    restart: unless-stopped
